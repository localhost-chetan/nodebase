{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/server.ts",
    "build": "bun build ./src/server.ts migrate.ts --target bun --outdir ./out --minify --production --drop=console",
    "db:generate": "drizzle-kit generate",
    "db:push": "bunx --bun drizzle-kit push"
  },
  "dependencies": {
    "better-auth": "^1.3.34",
    "drizzle-orm": "^0.44.7",
    "drizzle-query-logger": "^1.1.1",
    "hono": "^4.10.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.6"
  }
}